[project]
name = "aethernet-core"
version = "2.0.1"
description = "Full HTTP and WebSocket traffic over any message-based transport — strings or bytes."
authors = [
    { name = "Walter Kerrigan", email = "esolment@gmail.com" },
]
readme = "README.md"
requires-python = ">=3.12"
license = { file = "LICENSE" }
dependencies = [
    "basex-python==0.1.0",
    "pycryptodome>=3.23.0,<4",
    "msgpack>=1.2.1,<2",
    "httpx>=0.28.1,<0.29",
    "websockets>=16.0,<16.1",
    "pillow>=12.3,<13",
    "screeninfo>=0.8.1,<1",
    "pynput>=1.8.2,<2",
    "copykitten>=2.0.0,<3"
]

[project.optional-dependencies]
tests = [
    "pytest>=9.0,<10",
    "pytest-mock>=3.15,<4",
    "pytest-asyncio>=1.3,<2",
    "pytest-cov>=7.1,<8",
    "hypothesis>=6.152,<7",
]

lint = [
    "ruff>=0.15,<1",
    "black>=26.3,<27",
    "mypy>=1.20,<2",
]

build = [
    "twine>=6.2.0,<7",
    "build>=1.4.3,<2",
]

dev = [
    "aethernet-core[tests,lint,build]",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = [
    "-ra",
    "--strict-markers",
    "--cov=src",
    "--cov-report=term-missing",
    "--cov-fail-under=50"
]

[tool.coverage.run]
source = ["src"]
branch = true

[tool.mypy]
exclude = ["tests/"]
ignore_missing_imports = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/aethernet"]
